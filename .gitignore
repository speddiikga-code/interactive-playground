node_modules/
dist/
build/
.venv/
__pycache__/
*.pyc

# Secrets — never commit. preflight.sh also greps for key shapes.
.env
.env.*
!.env.example
*.pem
id_rsa*

# Claude Code local state (settings.json IS committed; local overrides are not)
.claude/settings.local.json

.DS_Store
